const raizQuadrada = (x) => {
    return x * x;
}

const raizQuadrada2 = n => n * n;

console.log(raizQuadrada(2));
console.log(raizQuadrada2(4));


const multPorDois = (y) => {
    return y * 5;
}

console.log(multPorDois(3));


const multPorDois2 = y => y * 5;
console.log(multPorDois2(2));